# React Biscuit Machine App

## Client

Client folder is utilizing create-react-ap and socket.io-client. You must use `npm start / yarn start` to load the application.

## Server

To run the server, you will need to use `npm start`.
